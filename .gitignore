# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-dev
dist-ssr
*.local

# Secrets — never commit
.env
.env.local
.env.*.local
.env.backup
.env.*.backup
*.env.backup

# Pipeline artifacts — per-run state (local only)
.automation/snapshots/
.automation/diffs/
.automation/reports/
.automation/logs/
.automation/backups/
.automation/demo-compare/
.automation/task-8-stage0/
.automation/viewer/
!.automation/**/.gitkeep

# Note: .automation/baseline/ is intentionally tracked.
# It is the canonical source-of-truth for diff comparison in CI.
# Updated only via approve+promote-dev workflow.

# Codex session state (local only)
.context/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tooling/session state caches (omc HUD)
.omc/
